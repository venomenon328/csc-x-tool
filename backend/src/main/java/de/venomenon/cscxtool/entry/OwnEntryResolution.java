package de.venomenon.cscxtool.entry;

/** Explicit state for the current user's existing contest entry in one show. */
public enum OwnEntryResolution {
    UNRESOLVED,
    NO_OWN_ENTRY,
    OWN_ENTRY
}
