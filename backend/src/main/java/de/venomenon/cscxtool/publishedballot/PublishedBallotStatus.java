package de.venomenon.cscxtool.publishedballot;

/** UNERFASST is a derived state; the other two values are persisted deliberately. */
public enum PublishedBallotStatus {
    UNERFASST,
    NICHT_ABGESTIMMT,
    ABGESTIMMT
}
